export default removeEmoji;
/**
 * Removes Emoji from a string
 *
 * @param {string} str - The string to convert.
 * @returns {string} - Returns the converted string.
 */
declare function removeEmoji(str: string): string;
